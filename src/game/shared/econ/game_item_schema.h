#ifndef GAME_ITEM_SCHEMA_H
#define GAME_ITEM_SCHEMA_H

#ifdef _WIN32
#pragma once
#endif

#include "econ_item_schema.h"

// Valve apparently also includes game specific files here like tf_item_schema.h but we don't really need that.

#endif // GAME_ITEM_SCHEMA_H
